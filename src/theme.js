import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00538C",
      dark: "#002244",
    },
    secondary: {
      main: "#0000CD",
    },
  },
});

export default theme;
